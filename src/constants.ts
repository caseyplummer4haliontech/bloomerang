/**
 * Shared constants used across analyzers.
 * Tune these values to adjust scoring and cost calculations.
 */

/** Minutes of developer time lost per context switch (e.g., waiting for a retry). */
export const CONTEXT_SWITCH_PENALTY_MIN = 15;
